# Database Rules for Codex
# Best practices for database design and operations

[database.schema]
description = "Schema design best practices"
patterns = [
    "Use snake_case for table and column names",
    "Always include id primary key (UUID or auto-increment)",
    "Add created_at and updated_at timestamps",
    "Use appropriate data types",
    "Normalize to 3NF, denormalize for performance only"
]

[database.indexing]
description = "Indexing strategy"
patterns = [
    "Index foreign keys and frequently queried columns",
    "Use composite indexes for multi-column queries",
    "Consider covering indexes for read-heavy workloads",
    "Monitor slow queries and add indexes accordingly"
]

[database.queries]
description = "Query best practices"
patterns = [
    "Use parameterized queries (prevent SQL injection)",
    "Prefer specific columns over SELECT *",
    "Use LIMIT or pagination for result sets",
    "Analyze with EXPLAIN",
    "Avoid N+1 queries"
]

[database.migrations]
description = "Migration management"
patterns = [
    "Use migration tools (Alembic, Flyway)",
    "Make migrations reversible",
    "Test on staging before production",
    "Keep migrations small and focused"
]

[database.connections]
description = "Connection management"
patterns = [
    "Use connection pooling",
    "Set appropriate timeouts",
    "Handle failures gracefully",
    "Use context managers for cleanup"
]
